{% extends 'layout/admin.base.twig' %}
{% block content %}
    <h2>Accueil</h2>
    <form novalidate action="{{ path('form_accueil') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" />
    </form>
{% endblock %}